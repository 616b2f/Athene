@model IEnumerable<Book>
@{
    Layout = "_Layout";
    ViewBag.Title = "Suche";
}

<h3>Büchersuche</h3>

@if (Model == null)
{
}
else if (Model.Count() == 0)
{
 <p>Nichts gefunden</p>
}
else if (Model.Count() > 0)
{
    @Model.Count() @:Treffer<br/>
    @foreach (var book in Model)
    {
        @Html.Partial("_BookPreview", book)
    }
}
