@model IEnumerable<Book>
@{
    Layout = "_Layout";
    ViewBag.Title = "Suche";
}

<h3>Büchersuche</h3>

@if (Model == null)
{
}
else if (Model.Count() == 0)
{	
	<div class="text-center">
		<br>
		<br>
		<h3><small>Es wurden keine Treffer gefunden</small></h3>
	</div>
}
else if (Model.Count() > 0)
{
    @Model.Count() @:Treffer<br/>
    @foreach (var book in Model)
    {
        @Html.Partial("_BookPreview", book)
    }
}
