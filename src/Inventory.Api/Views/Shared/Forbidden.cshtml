@{
    ViewData["Title"] = Localizer["Forbidden"];
}

<h1 class="text-danger">@Localizer["Forbidden"]</h1>
<h2 class="text-danger">@Localizer["Your are not allowed to access this region of the site."]</h2>